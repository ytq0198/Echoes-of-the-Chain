import { GradeContract } from './grade-contract';

export { GradeContract } from './grade-contract';
export { AcademicRecordError } from './lib/errors';
export type { AppealDecision, AppealRecord, CredentialDraftInput, CredentialRecord } from './model';

export const contracts = [GradeContract];
