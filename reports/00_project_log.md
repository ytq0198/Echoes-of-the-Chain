# 项目进程日志

## 2026-08-25：项目启动与立项调研

### 已完成

- 读取并视觉核对课程大作业要求及参考选题。
- 核验 CCF 第五届竞赛官网、赛制、提交物、匿名要求及安全/隐私导向。
- 调研教育凭证、成绩记录、选择性披露相关论文、标准与开源项目。
- 只读盘点本机、GitHub 空仓库和学校服务器环境。
- 建立 `design/` 与 `reports/` 两类持续文档目录。
- 形成三个候选方向和推荐路线，等待队长决策。

### 环境快照

- 本地初始目录仅有两份课程 PDF，尚未初始化 Git。
- GitHub 远端无引用，确认为空仓库。
- 服务器主机 `a6000-9961`：Ubuntu 22.04、96 CPU、251 GiB 内存。
- `/mnt/localDisk3` 可用约 2.0 TiB；Docker 28.1.1、Compose 2.35.1 可用。
- 服务器当前未发现系统级 Node.js/Go，后续优先通过容器或项目专属环境提供，避免修改全局环境。

### 待决策

- 主方向与项目中文名称。
- 指导教师信息（报名阶段需要，当前开发不阻塞）。
- 是否在首版纳入成绩申诉流程；建议纳入轻量版本。

### 下一阶段入口条件

队长确认方向后，输出需求规格、角色权限矩阵、威胁模型、链上/链下数据边界、Fabric 网络拓扑、迭代计划和三人分工，并开始技术尖峰。

## 2026-08-25：方向冻结与 Iteration 0 启动

### 决策

- 队长确认采用方向 A“隐私保护型可信学业凭证”。
- 纳入轻量成绩修订与申诉流程。
- 工作产品名暂定 `ChainGrade`，后续可只修改品牌而不改变领域模型。
- 课程答辩与竞赛提交始终对应同一个项目；只允许在同一主线上连续增强，不建立两套产品或长期分叉代码。

### 本阶段目标

- 完成产品需求、权限矩阵、威胁模型、数据边界、系统架构和三人分工。
- 建立 TypeScript monorepo 和首个链码垂直切片。
- 以测试证明角色隔离、提交/复核职责分离和不可变修订链。

### Iteration 0 首个垂直切片结果

- 已完成统一项目需求、威胁模型、架构和团队计划。
- 已建立 Web、API、共享类型和 Fabric 链码 monorepo。
- 已实现成绩签发、独立复核、修订、撤销、验证和申诉的链码核心。
- 已改用每组织隐式私有集合，避免模拟高校之间默认共享成绩明文。
- 本地类型检查、测试、生产构建及桌面/移动浏览器渲染均通过。
- 学校服务器项目专属运行时复验通过；因服务器到 GitHub 临时网络超时，本轮使用 Git bundle 经 SSH 快进同步。

## 2026-08-25：真实 Fabric 与 Gateway 垂直闭环

### 已完成

- 在服务器项目目录内固定 Fabric 2.5.16 LTS、Fabric CA 1.5.17、jq 1.7.1 与 samples commit。
- 建立双组织 Raft/LevelDB 开发网络和 `chaingrade` 通道，签发 issuer、reviewer、student 属性证书。
- 使用隔离 staging 打包 TypeScript 链码，完成双组织安装、批准、commit 与一次链码升级。
- 实现 Fastify 到 Fabric Gateway 的成绩提交、复核、读取和验真路径。
- 真实提交 `cred:2026:real01`，复核后状态为 `ACTIVE`；正确哈希验真为 true，错误哈希为 false。
- 服务器与本地全量类型检查、18 项测试和生产构建通过。

### 关键修复

- 明确拉取并本地标记固定版本镜像，避免 samples 的 `latest` 漂移。
- 将 jq、Node、pnpm 全部限制在项目 `.tools/`，不修改服务器系统环境。
- 隔离 pnpm workspace 链接，避免链码容器缺失 `fabric-chaincode-node`。
- 删除链码公开交易参数的默认值，修复 Fabric Contract API 运行时参数反射。

### 下一步

- 实现教师、复核员、学生和公开验证者的 Web 应用壳与真实 API 对接。
- 将修订/申诉链码能力接入 Gateway API，并补充身份会话与结构化错误映射。

## 2026-08-25：四角色工作台与真实浏览器闭环

### 已完成

- 将产品总览与教师签发、独立复核、学生凭证、公开验真四类工作台整合为同一个 Vue 单页应用。
- 接通教师草稿提交、复核员查询/批准、学生状态查询和公开哈希验真 API。
- 以合成凭证 `cred:2026:web01` 完成真实浏览器—Fastify—Gateway—Fabric 闭环；最终状态为 `ACTIVE`，账本高度从 11 增至 13。
- 对正确详情哈希验证为真实有效，对错误哈希显示明确警告。
- 完成 1280×720 桌面端及 390×844 移动端验收；移动端无横向溢出，浏览器控制台无警告或错误。
- 本机 18 项测试、类型检查和生产构建通过。

### 产品边界

- 申诉按钮保留在学生信息架构内，并明确标记为下一阶段，不伪装未接通功能。
- 当前工作台通过后端固定动作映射 Fabric 身份；正式终端登录、学生本人属性校验和业务错误码仍待实现。

### 下一步

- 接入成绩修订与轻量申诉 Gateway API，并升级真实链码定义。
- 增加角色会话、学生本人查询授权、稳定业务错误映射及可重复的浏览器自动化脚本。

## 2026-08-25：不可变修订与轻量申诉闭环

### 已完成

- 接通成绩修订、学生申诉、申诉读取和复核结论 Gateway API。
- 将 student 属性证书加入后端固定身份映射，链码继续校验 `app.role=student`、组织和 `subject.hash`。
- 申诉理由与复核结论都以 transient data 写入 Org1 隐式私有集合，公共账本仅保留 SHA-256 承诺。
- 将链码升级为 `grade 0.3 / sequence 3`，Org1/Org2 均批准，账本状态连续保留。
- 真实修订 `cred:2026:web01-v2` 激活为 v2，旧版 `cred:2026:web01` 原子变为 `SUPERSEDED`。
- 学生提交 `appeal:2026:web01`，复核后状态为 `RESOLVED_ACCEPTED`。
- 真实负向测试发现 Gateway 业务码位于 peer details，修复后失效版本申诉稳定返回 `409 INVALID_STATE`。
- 25 项自动测试、类型检查、构建和链码覆盖率检查通过；浏览器移动端无横向溢出或控制台错误。

### 下一步

- 实现终端会话与角色登录，避免演示前端依赖固定后端身份选择。
- 增加申诉待办/凭证列表查询模型、私有详情授权读取及可重复浏览器 E2E。
- 设计竞赛展示所需的二维码验证和选择性披露证明。

## 2026-08-25：角色会话与学生本人私有披露

### 已完成

- 增加 issuer、reviewer、student 三类短期 HMAC 签名会话，真实密码与会话密钥仅由运行环境注入。
- 会话 Cookie 使用 HttpOnly、SameSite=Strict、Path=/；HTTPS 模式使用 Secure 与 `__Host-` 前缀。
- 所有登录后状态变更增加角色、Origin/Sec-Fetch-Site 与 CSRF 自定义头校验。
- 链码新增 `ReadPrivateCredential`，同时校验 student 角色、同组织和证书 subject.hash。
- 学生私有详情接口增加 `Cache-Control: no-store`，公开验真继续免登录。
- 将链码升级为 `grade 0.4 / sequence 4`，Org1/Org2 双方批准且历史账本连续。
- 真实验证缺失 CSRF、错误角色、跨站来源、非学生私有读取均被拒绝；学生本人读取 v2 成绩 95/A 成功。
- 临时测试 Cookie 曾出现在诊断输出中，立即轮换进程会话密钥使全部旧令牌失效。
- 桌面与 390×844 移动登录门通过视觉验收，无横向溢出或控制台警告。

### 下一步

- 引入面向竞赛部署的正式身份提供方、密码限速/MFA 与持久会话撤销机制。
- 建立凭证/申诉待办查询模型及可重复浏览器 E2E。
- 实现二维码验真与最小披露证明，准备课程答辩故事线和竞赛演示脚本。
