# 项目进程日志

## 2026-08-25：项目启动与立项调研

### 已完成

- 读取并视觉核对课程大作业要求及参考选题。
- 核验 CCF 第五届竞赛官网、赛制、提交物、匿名要求及安全/隐私导向。
- 调研教育凭证、成绩记录、选择性披露相关论文、标准与开源项目。
- 只读盘点本机、GitHub 空仓库和学校服务器环境。
- 建立 `design/` 与 `reports/` 两类持续文档目录。
- 形成三个候选方向和推荐路线，等待队长决策。

### 环境快照

- 本地初始目录仅有两份课程 PDF，尚未初始化 Git。
- GitHub 远端无引用，确认为空仓库。
- 服务器主机 `a6000-9961`：Ubuntu 22.04、96 CPU、251 GiB 内存。
- `/mnt/localDisk3` 可用约 2.0 TiB；Docker 28.1.1、Compose 2.35.1 可用。
- 服务器当前未发现系统级 Node.js/Go，后续优先通过容器或项目专属环境提供，避免修改全局环境。

### 待决策

- 主方向与项目中文名称。
- 指导教师信息（报名阶段需要，当前开发不阻塞）。
- 是否在首版纳入成绩申诉流程；建议纳入轻量版本。

### 下一阶段入口条件

队长确认方向后，输出需求规格、角色权限矩阵、威胁模型、链上/链下数据边界、Fabric 网络拓扑、迭代计划和三人分工，并开始技术尖峰。

## 2026-08-25：方向冻结与 Iteration 0 启动

### 决策

- 队长确认采用方向 A“隐私保护型可信学业凭证”。
- 纳入轻量成绩修订与申诉流程。
- 工作产品名暂定 `ChainGrade`，后续可只修改品牌而不改变领域模型。
- 课程答辩与竞赛提交始终对应同一个项目；只允许在同一主线上连续增强，不建立两套产品或长期分叉代码。

### 本阶段目标

- 完成产品需求、权限矩阵、威胁模型、数据边界、系统架构和三人分工。
- 建立 TypeScript monorepo 和首个链码垂直切片。
- 以测试证明角色隔离、提交/复核职责分离和不可变修订链。

### Iteration 0 首个垂直切片结果

- 已完成统一项目需求、威胁模型、架构和团队计划。
- 已建立 Web、API、共享类型和 Fabric 链码 monorepo。
- 已实现成绩签发、独立复核、修订、撤销、验证和申诉的链码核心。
- 已改用每组织隐式私有集合，避免模拟高校之间默认共享成绩明文。
- 本地类型检查、测试、生产构建及桌面/移动浏览器渲染均通过。
- 学校服务器项目专属运行时复验通过；因服务器到 GitHub 临时网络超时，本轮使用 Git bundle 经 SSH 快进同步。

## 2026-08-25：真实 Fabric 与 Gateway 垂直闭环

### 已完成

- 在服务器项目目录内固定 Fabric 2.5.16 LTS、Fabric CA 1.5.17、jq 1.7.1 与 samples commit。
- 建立双组织 Raft/LevelDB 开发网络和 `chaingrade` 通道，签发 issuer、reviewer、student 属性证书。
- 使用隔离 staging 打包 TypeScript 链码，完成双组织安装、批准、commit 与一次链码升级。
- 实现 Fastify 到 Fabric Gateway 的成绩提交、复核、读取和验真路径。
- 真实提交 `cred:2026:real01`，复核后状态为 `ACTIVE`；正确哈希验真为 true，错误哈希为 false。
- 服务器与本地全量类型检查、18 项测试和生产构建通过。

### 关键修复

- 明确拉取并本地标记固定版本镜像，避免 samples 的 `latest` 漂移。
- 将 jq、Node、pnpm 全部限制在项目 `.tools/`，不修改服务器系统环境。
- 隔离 pnpm workspace 链接，避免链码容器缺失 `fabric-chaincode-node`。
- 删除链码公开交易参数的默认值，修复 Fabric Contract API 运行时参数反射。

### 下一步

- 实现教师、复核员、学生和公开验证者的 Web 应用壳与真实 API 对接。
- 将修订/申诉链码能力接入 Gateway API，并补充身份会话与结构化错误映射。

## 2026-08-25：四角色工作台与真实浏览器闭环

### 已完成

- 将产品总览与教师签发、独立复核、学生凭证、公开验真四类工作台整合为同一个 Vue 单页应用。
- 接通教师草稿提交、复核员查询/批准、学生状态查询和公开哈希验真 API。
- 以合成凭证 `cred:2026:web01` 完成真实浏览器—Fastify—Gateway—Fabric 闭环；最终状态为 `ACTIVE`，账本高度从 11 增至 13。
- 对正确详情哈希验证为真实有效，对错误哈希显示明确警告。
- 完成 1280×720 桌面端及 390×844 移动端验收；移动端无横向溢出，浏览器控制台无警告或错误。
- 本机 18 项测试、类型检查和生产构建通过。

### 产品边界

- 申诉按钮保留在学生信息架构内，并明确标记为下一阶段，不伪装未接通功能。
- 当前工作台通过后端固定动作映射 Fabric 身份；正式终端登录、学生本人属性校验和业务错误码仍待实现。

### 下一步

- 接入成绩修订与轻量申诉 Gateway API，并升级真实链码定义。
- 增加角色会话、学生本人查询授权、稳定业务错误映射及可重复的浏览器自动化脚本。

## 2026-08-25：不可变修订与轻量申诉闭环

### 已完成

- 接通成绩修订、学生申诉、申诉读取和复核结论 Gateway API。
- 将 student 属性证书加入后端固定身份映射，链码继续校验 `app.role=student`、组织和 `subject.hash`。
- 申诉理由与复核结论都以 transient data 写入 Org1 隐式私有集合，公共账本仅保留 SHA-256 承诺。
- 将链码升级为 `grade 0.3 / sequence 3`，Org1/Org2 均批准，账本状态连续保留。
- 真实修订 `cred:2026:web01-v2` 激活为 v2，旧版 `cred:2026:web01` 原子变为 `SUPERSEDED`。
- 学生提交 `appeal:2026:web01`，复核后状态为 `RESOLVED_ACCEPTED`。
- 真实负向测试发现 Gateway 业务码位于 peer details，修复后失效版本申诉稳定返回 `409 INVALID_STATE`。
- 25 项自动测试、类型检查、构建和链码覆盖率检查通过；浏览器移动端无横向溢出或控制台错误。

### 下一步

- 实现终端会话与角色登录，避免演示前端依赖固定后端身份选择。
- 增加申诉待办/凭证列表查询模型、私有详情授权读取及可重复浏览器 E2E。
- 设计竞赛展示所需的二维码验证和选择性披露证明。

## 2026-08-25：角色会话与学生本人私有披露

### 已完成

- 增加 issuer、reviewer、student 三类短期 HMAC 签名会话，真实密码与会话密钥仅由运行环境注入。
- 会话 Cookie 使用 HttpOnly、SameSite=Strict、Path=/；HTTPS 模式使用 Secure 与 `__Host-` 前缀。
- 所有登录后状态变更增加角色、Origin/Sec-Fetch-Site 与 CSRF 自定义头校验。
- 链码新增 `ReadPrivateCredential`，同时校验 student 角色、同组织和证书 subject.hash。
- 学生私有详情接口增加 `Cache-Control: no-store`，公开验真继续免登录。
- 将链码升级为 `grade 0.4 / sequence 4`，Org1/Org2 双方批准且历史账本连续。
- 真实验证缺失 CSRF、错误角色、跨站来源、非学生私有读取均被拒绝；学生本人读取 v2 成绩 95/A 成功。
- 临时测试 Cookie 曾出现在诊断输出中，立即轮换进程会话密钥使全部旧令牌失效。
- 桌面与 390×844 移动登录门通过视觉验收，无横向溢出或控制台警告。

### 下一步

- 引入面向竞赛部署的正式身份提供方、密码限速/MFA 与持久会话撤销机制。
- 建立凭证/申诉待办查询模型及可重复浏览器 E2E。
- 实现二维码验真与最小披露证明，准备课程答辩故事线和竞赛演示脚本。

## 2026-08-25：固化 UI 截图验收规范

- 将真实浏览器视觉验收设置为每个前端阶段的强制关闭条件。
- 固定检查 1280×720 桌面端和 390×844 移动端，以及正常、空、错误、登录/权限和关键结果状态。
- 要求检查横向溢出与 console warning/error，确认无误后再保存截图。
- 截图统一存入 `reports/assets/iteration-N/` 并直接嵌入对应实验报告，禁止包含密码、令牌或私有成绩明文。
- 为 Iteration 3 补存首页、学生登录门桌面/移动端和公开验真共 4 张验收截图。

## 2026-08-26：可信成绩工作台视觉重设计

- 参考 VEGA Design Guidelines 的可信、技术、克制设计原则，重构 ChainGrade 视觉系统，但不复制其品牌资产。
- 首页升级为工作台信息架构，集中显示角色入口、Fabric 配置、凭证/申诉状态和隐私边界。
- 引入官方 Phosphor Vue 图标，统一按钮、卡片、表单、状态标签和响应式间距。
- 清除 UI 中“同一个项目”、课程答辩、竞赛、Iteration、演示账号及虚构人物数据等提示词痕迹。
- 1536×1024 桌面端完整首屏、390×844 移动端及折叠菜单通过浏览器视觉验收，console warning/error 为 0。
- 完成设计稿与浏览器实现的同视口/重点区域对比，修复页脚首屏外溢后 Design QA 通过。

## 2026-08-26：完整角色界面与二维码验真

- 将工作台视觉系统扩展到教师、复核、学生、公开验真、登录门和权限不匹配等全部界面状态。
- 自托管 Noto Serif SC Variable，为大型中文标题建立具有学术档案感的展示字形；正文、表单和哈希继续保持高可读性。
- 修复旧网格把复核详情与学生凭证挤入左侧窄栏的问题，建立统一角色说明列与业务内容列。
- 接入服务器真实 Fabric 数据完成浏览器验收：ACTIVE 凭证、RESOLVED_ACCEPTED 申诉、本人私有成绩、正确与错误哈希均正常呈现。
- 新增学生二维码验真入口，二维码只携带凭证标识和详情哈希；扫码落地页可自动恢复验真字段。
- 390×844 移动端页面和流程导航均无横向溢出，浏览器 console warning/error 为 0。
- 设计对比在修复布局与移动导航 P1/P2 问题后通过，截图与完整过程写入 `reports/08_complete_ui_and_qr_e2e.md`。

## 2026-08-26：角色化链上任务队列

- 为凭证和申诉增加状态、学生归属四类 Fabric 复合键索引，状态转换时在同一交易内迁移索引。
- 增加教师签发记录、凭证复核队列、学生本人凭证、申诉复核队列和学生本人申诉五个角色化分页查询。
- 增加受 reviewer 证书保护的历史索引重建交易，真实迁移 3 条凭证和 1 条申诉。
- 真实 LevelDB 验收发现 Node shim 分页 metadata 缺失，改为链码生成复合键 Base64URL 游标；该问题在 mock 测试中不可见。
- 链码最终升级为 `grade 0.6 / sequence 6`，Org1/Org2 均安装、批准并查询到一致定义。
- reviewer 真实读取 2 条 ACTIVE；student 真实读取 3 条本人凭证和 1 条已接受申诉；student 调 reviewer 队列被链码拒绝。
- 教师、复核员、学生桌面端和 390×844 移动端通过浏览器视觉验收，7 张截图写入 `reports/09_role_scoped_ledger_queues_e2e.md`。

## 2026-08-26：私有驳回与撤销闭环

- 为待复核凭证增加驳回、为有效凭证增加撤销，完成批准/驳回/撤销状态机。
- 复核决定正文通过 transient data 写入 reviewer 所在组织隐式私有集合，公共账本只记录 SHA-256 理由承诺。
- API 增加 reviewer 会话、同源和 CSRF 保护的决定路由；链码再次校验 transient 内容与公开哈希一致。
- 链码升级为 `grade 0.7 / sequence 7`，Org1/Org2 均批准且两个 peer 定义一致。
- 真实完成 `cred:2026:reject-e2e07` 驳回和 `cred:2026:revoke-e2e07` 激活后撤销；重复撤销返回 409，公共结果不包含理由明文。
- 37 项自动测试、类型检查和 Web 构建通过。
- 桌面端驳回/撤销审计与 390×844 移动端撤销表单通过视觉验收，无横向溢出、浏览器 console warning/error 为 0。
- 完整证据与截图写入 `reports/10_private_credential_decisions_e2e.md`。

## 2026-08-27：限时最小字段披露与运行模式可信标识

### 防跑偏检查

- 主线仍是成绩存证、查询、修订/申诉和学生授权披露，符合课程大作业要求，并为同一项目的竞赛展示增加隐私价值。
- 明确不把普通二维码验真包装为“选择性披露”，也不声称当前方案具备 BBS+、零知识证明或跨校互认能力。
- 本阶段暂不扩展身份提供方、跨校网络或复杂密码学证明，避免在核心闭环完成前横向发散。

### 已完成

- 链码增加绑定字段、用途、验证者、有效期和最大次数的披露授权；公共状态只保存 token、用途和验证者哈希。
- 授权访问参数通过 transient data 传入，链码先校验再读取隐式私有集合；成功消费原子递增次数，达到上限后转为 `CONSUMED`。
- 学生工作台增加授权创建、一次性链接、历史记录与撤销；公开验真增加授权字段核验，令牌输入保持掩码。
- 增加只在 `DEMO_ENABLED=true` 时启用的进程内演示账本，并由 API 返回 `ledgerMode`；UI 明确区分真实 Fabric、离线演示和未连接状态。
- 修正首页静态样例误称“当前记录”的问题，统一标为“流程示意”。
- 46 项自动测试、类型检查和生产构建通过；真实 HTTP 负向测试确认错误验证者为 403、超次使用为 409，公共记录不泄漏用途、验证者或 token 明文。
- 五个页面在 390×844 下均无横向溢出，浏览器 console warning/error 为 0；桌面与移动截图写入 `reports/assets/iteration-8/`。

### 环境事件与未关闭项

- 学校服务器在阶段开始前发生重启，项目仓库和 Fabric 身份材料保留，但 Docker 容器、卷和镜像运行态丢失，因此旧开发账本不可恢复。
- 重建时 Docker 守护进程返回 `layer does not exist`，连镜像 inspect 与 `docker system df` 均失败，判定为共享 Docker 元数据损坏；未擅自清理共享 Docker 根目录。
- 代码与离线 E2E 已完成，`grade 0.8` 的服务器真实 Fabric 部署与交易证据须在管理员修复 Docker 后补验；报告未把演示账本结果表述为链上结果。

## 2026-08-27：原子 CSV 成绩批量导入

### 防跑偏检查

- 批量录入直接补齐成绩管理应用的教学班工作流，继续进入既有复核、查询、修订/申诉和披露链路。
- 课程答辩与竞赛提交仍使用同一个 ChainGrade 产品、仓库、状态机和部署体系。
- 本阶段不扩展无关教务模块，也不提前进入 BBS+、零知识证明或跨校联盟互认。

### 已完成

- 增加支持 BOM、CRLF、引号与行号错误提示的共享 CSV 解析器，以及浏览器和 API 双重批校验。
- 链码新增 `CreateCredentialBatch`，在所有公开状态、复合索引和隐式私有数据写入前完成整批预检；1–50 条记录共享同一 transaction ID。
- API 新增 issuer + 同源 + CSRF 保护的批量导入路由，Gateway 只提交一次且响应不含课程名、分数、盐值或 CSV 原文。
- 教师工作台增加本地预检、提交预览、错误阻断和原子交易结果卡，并沿用完整界面视觉系统。
- 浏览器首次加载发现并行构建会让 Web 读取旧 shared `dist`；根构建脚本已调整为显式依赖顺序，重新构建后空白页与缺失导出错误消失。
- 52 项自动测试、类型检查、生产构建和链码覆盖率检查通过；混合冲突批次返回 409，新凭证查询为 404，未出现部分写入。
- 1440×900 桌面端与 390×844 移动端正常态、成功态和错误态通过浏览器验收；五页移动布局无横向溢出，console warning/error 为 0。
- 四张最终截图写入 `reports/assets/iteration-9/`，完整证据写入 `reports/12_atomic_csv_import_e2e.md`。

### 环境事件与未关闭项

- 学校服务器共享 Docker 元数据的 `layer does not exist` 故障仍待管理员处理，本阶段未执行可能影响其他用户的全局清理。
- `grade 0.9 / sequence 1` 的真实 Fabric 部署、双 peer committed definition 与真实批量 transaction ID 仍须在 Docker 恢复后补验；当前报告只把本地 HTTP 流程称为离线演示账本验收。

## 2026-08-27：无 Docker 原生 Fabric 恢复

- 共享 Docker 的 `layer does not exist` 未修复，因此建立完全位于 `/mnt/localDisk3/weizian` 的原生 Fabric 2.5.16 备用运行方式。
- 对保留的 peer/orderer/应用 MSP、TLS 和通道材料制作 `0600` 权限、SHA-256 校验通过的恢复归档。
- 新增原生环境预检、节点启动/停止/状态/受限 reset 和 CCAAS 链码生命周期脚本；全流程不调用 Docker。
- 原生 orderer 成功创建新 `chaingrade` 通道并当选 Raft leader，Org1/Org2 peer 均加入通道。
- `grade 0.9 / sequence 1` 以 Node.js Chaincode-as-a-Service 提交，Org1MSP/Org2MSP 批准均为 true，commit 在两个 peer 均为 VALID。
- 两个 peer 最终高度均为 9，current/previous block hash 完全一致，恢复后日志无 panic。
- 真实 Gateway 完成 issuer 创建、reviewer 批准和公开验真；ACTIVE 交易 ID 为 `c7f5827e87f1a1964e89d38b8621aa255f5aa3369f9853422a48c5e92becf7a7`。
- 真实原子批量导入两条记录共享交易 `fe1cd817308b9a18fb4375a9f53f99c97f9ec9cc1e971f66b4fef11a378c0cce`；混合冲突返回 409，批内新记录仍为 404。
- 临时无认证 Gateway 仅监听 localhost，验收结束后已停止；原生网络和外部链码继续运行。
- 旧 Docker volume 的历史 transaction ID 未恢复，本次证据明确属于新恢复网络，不与旧账本混淆。

## 2026-08-27：原生 Fabric 可运维化与鉴权 UI 复验

### 防跑偏检查

- 所有改动继续围绕成绩凭证签发、复核、查询、修订/申诉和授权披露，不新增无关教务系统。
- 课程答辩和竞赛使用同一个 ChainGrade 项目；可恢复部署与确定性播种是同一项目的工程化能力。
- 不把新原生账本表述为旧 Docker volume 的历史恢复，不修改共享 Docker 或学校服务器内核参数。

### 已完成

- 链码 `deploy` 增加已提交定义检测，新增只启动既有 CCAAS 的 `start`，重复部署不再重复 sequence 1 生命周期交易。
- 完整停机与启动后双 Peer 高度 9、current/previous block hash 与重启前完全一致。
- 新增双 Peer 一致性查询、冷备份、SHA-256/路径安全验证和带显式确认的受保护恢复脚本。
- 生成 `0600` 原生账本归档并完成真实恢复演练；恢复前运行目录保留为时间戳回退副本。
- 新增确定性演示播种器，首次创建并激活凭证、提交 OPEN 申诉；第二次运行前后账本高度 12 与哈希不变。
- 真实 Fabric + AUTH 模式下，401/403 负向用例、三类角色工作台、学生私有成绩和公开验真均通过。
- 1280×720 桌面和 390×844 移动端完成浏览器视觉验收，无横向溢出，console warning/error 为 0；登录门、角色不匹配、教师、复核员、学生、公开验真及移动导航共保留 15 张证据截图。
- 学校服务器 watcher 额度耗尽时仅为项目 Vite 启用 polling，没有修改共享系统参数。
- 2026-08-28 再次只读检查 Docker：daemon 可连接但仍报告 0 容器/0 镜像，Fabric peer 镜像 inspect 继续返回 `layer does not exist`，故障未恢复。
- 设计、恢复证据和 10 张截图写入 `design/17_native_operations_and_demo_readiness.md` 与 `reports/14_native_operations_auth_ui_e2e.md`。

### 下一步

- 编写一键演示运行手册、课程答辩故事线和竞赛材料骨架。
- 对答辩流程做定时彩排，记录冷启动时长与故障回退路径。
- Docker 修复后保留容器部署作为第二路径，但不再把它作为项目继续开发的前置条件。
