# 第 20 阶段：实验报告上游模板对齐修复

## 1. 问题与修复结果

用户导出的修复前 PDF 共 22 页。实际渲染前四页后发现三个问题：封面是项目自定义文字版而非上游浙江大学校徽模板；目录同时显示 CSS 自动编号和正文手写编号；目录后出现一张完整空白页。

本阶段撤销了第 19 阶段的大部分自定义视觉规则，恢复以上游 `typora-latex-theme` v0.3.3 为主的封面和正文排版。修复只填写项目资料、加入用户确认的学号 `3240101782`、本地化两个 SVG，并保留两条兼容规则。

## 2. 根因与对应改动

| 可观察问题 | 已验证根因 | 修复 |
| --- | --- | --- |
| 封面无校徽 | Markdown 使用自定义 `cover-school` 文字结构 | 替换为上游校名 SVG、校徽 SVG、标题、类型和信息表顺序 |
| 目录出现 `1  1.` | 上游伪元素选择器作用在 `.md-toc-h2::before`，旧覆盖层误写为子元素 `a::before` | 使用与上游一致的伪元素选择器关闭自动编号 |
| 目录后完整空白页 | 空分页元素在目录跨页后自身落到下一页，再执行 `page-break-after` | 删除空元素，让第一章 H2 使用 `break-before: page` |
| Typora 仍可能显示旧样式 | 应用读取用户主题目录中的 CSS 副本 | 同步更新用户主题目录并核对 SHA-256 |

## 3. 自动校验

| 校验项 | 结果 |
| --- | ---: |
| 正文与原报告逐字比较 | 一致 |
| 用户学号 | `3240101782` 已出现 |
| 本地校名/校徽引用 | 2 / 2 |
| 全文图片引用 | 9 个 |
| 缺失图片 | 0 个 |
| 两个 SVG XML 解析 | 通过 |
| 旧自定义封面类 | 0 个 |
| 旧空分页元素 | 0 个 |
| 项目 CSS 与 Typora 已安装 CSS | SHA-256 一致 |

## 4. 证据边界与下一步

项目目录中的旧 PDF 是用户在修复前导出的基线文件，本阶段没有覆盖或删除它，因此它仍会显示旧封面、重复编号和空白页。必须完全关闭并重新打开 Typora，选择 `chaingrade-latex`，再重新导出一个 PDF 才能完成修复后逐页视觉验收。

当前自动校验支持“源文件已按上游模板对齐、依赖完整、正文未变”的结论；在新的 PDF 被导出并渲染检查前，不把最终分页效果写成已经视觉验收通过。
