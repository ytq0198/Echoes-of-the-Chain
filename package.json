{
  "name": "echoes-of-the-chain",
  "version": "0.1.0",
  "private": true,
  "description": "ChainGrade privacy-preserving academic credential platform on Hyperledger Fabric",
  "packageManager": "pnpm@11.19.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm --filter @chaingrade/shared build && pnpm --filter @chaingrade/grade-contract build && pnpm --filter @chaingrade/api build && pnpm --filter @chaingrade/web build",
    "check": "pnpm --filter @chaingrade/shared build && pnpm -r --if-present check",
    "test": "pnpm --filter @chaingrade/shared build && pnpm -r --if-present test",
    "test:coverage": "pnpm -r --if-present test:coverage",
    "dev:api": "pnpm --filter @chaingrade/api dev",
    "dev:web": "pnpm --filter @chaingrade/web dev"
  },
  "devDependencies": {
    "prettier": "3.9.6",
    "typescript": "5.9.3"
  }
}
